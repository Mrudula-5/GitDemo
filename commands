git --version

git config --global user.name "Mrudula Sonawane"
git config --global user.email "sonawanemrudula5@gmail.com"

git config --global -e

cd ~/OneDrive/Desktop
mkdir GitDemo
cd GitDemo

git init

echo "Welcome to Git Demo" > welcome.txt

ls
cat welcome.txt

git status

git add welcome.txt

git commit

git remote add origin https://github.com/Mrudula-5/GitDemo.git

git remote -v

git push -u origin main
